version: 0.2

phases:
  install:
    commands:
      - echo NPM install...
      - npm install
#  pre_build:
#    commands:
#      - echo Installing source NPM dependencies...
#      - npm install
#  build:
#    commands:
#      - echo Build started on `date`
#      - run build
#  post_build:
#    commands:
#      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'